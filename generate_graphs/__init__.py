"""
Params
    N = number of nodes
    A = expected average density of edges
Return
    adjacency_list = List[List[Boolean]],
    radius = Float,
    total_edges = Int,
    average_degree = Float,
    max_degree = Int,
    min_degree = Int,
"""
from .generate_square import unit_square_graph, calculate_radius_square
